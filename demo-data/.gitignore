# Generated PDFs (binary, regenerable via generate.py — reproduced byte-identically).
out/*.pdf
# The packaged asset (hosted as a release download, not committed).
demo-vault.zip
# Local venv / caches.
.venv/
__pycache__/
*.pyc
